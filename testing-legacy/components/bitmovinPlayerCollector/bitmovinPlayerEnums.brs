' Player controls
function getPlayerControls()
  controlStates = {
    PLAY: "play",
    INSTANT_REPLAY: "replay",
    RESUME: "resume",
    PAUSE: "pause",
    PREBUFFER: "prebuffer",
    run: "run",
    stop: "stop"
  }
  return controlStates
end function
